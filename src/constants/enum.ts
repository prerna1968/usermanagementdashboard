export enum AppRoutes {
    HOME = "/",
    SIGNUP = "/signup",
    SIGNIN = "/signin",
    DASHBOARD = "/dashboard",
  }
  
  export enum apiEndPoints {
    SIGNIN = "/login",
    SIGNUP = "/register",
    USERS = "/users",
    LOGOUT = "/logout",
  }